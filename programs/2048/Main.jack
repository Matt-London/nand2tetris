/**
 * Main class to handle running the game
 *
 * @author Matt London
 */
class Main {
    function void main() {
        var Board board;

        let board = Board.new(10, 10, 200, 200);

        do board.drawBoard();

        return;
    }
}